#mybatis
mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
  typeAliasesPackage: com.example.demo.sys.dao
logging:
  level: warn
#logging

spring:
  devtools:
    restart:
      enabled: true
  profiles:
    active: dev